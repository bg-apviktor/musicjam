// Tailwind CSS v4 — theme configuration is in src/styles/globals.css via @theme
// This file is kept for plugin compatibility only.
import type { Config } from "tailwindcss";

const config: Config = {};

export default config;
